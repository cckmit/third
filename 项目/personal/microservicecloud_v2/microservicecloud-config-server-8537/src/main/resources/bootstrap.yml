server:
  port: 8537

spring:
  application:
    name: microservicecloud-config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:beitieforerver/third.git
      label: master

  rabbitmq:
    username: qingfeng
    password: bestbnf
    host: localhost
    port: 5672
management:
  endpoints:
    web:
      exposure:
        include: 'bus-refresh'