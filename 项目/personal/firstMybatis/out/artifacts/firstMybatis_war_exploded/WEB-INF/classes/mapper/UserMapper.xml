<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
 mapper为映射的根节点，namespace指定Dao接口的完整类名
mybatis会依据这个接口动态创建一个实现类去实现这个接口，
而这个实现类是一个Mapper对象-->
<mapper namespace="com.beitie.dao.UserMapper">
    <!-- 根据uid查询一个用户信息 -->
    <select id="selectUserById" parameterType="Integer"
            resultType="com.beitie.bean.User">
        select * from users where uid = #{uid}
    </select>
    <!-- 查询所有用户信息 -->
    <select id="selectAllUser" resultType="User">
        select * from users
    </select>
    <!-- 添加一个用户，#{uname}为 com.mybatis.po.MyUser 的属性值 -->
    <insert id="addUser" parameterType="user" useGeneratedKeys="true">
        insert into users (uid,uname,uage)
        values(#{uid},#{uname},#{uage})
    </insert>
    <!--修改一个用户 -->
    <update id="updateUser" parameterType="user" useGeneratedKeys="true">
        update users set uname =
        #{uname},uage = #{uage} where uid = #{uid} order by uid ;
    </update>
    <!-- 删除一个用户 -->
    <delete id="deleteUser" parameterType="user" >
        delete from users where uname
        = #{uname} and uid != #{uid};
    </delete>
</mapper>