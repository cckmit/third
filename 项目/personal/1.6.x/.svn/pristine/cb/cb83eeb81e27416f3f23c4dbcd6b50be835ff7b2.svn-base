package org.opoo.apps.file;

import java.io.File;

/**
 * 文件更新器。
 * 
 * @author Alex Lin(alex@opoo.org)
 *
 */
public interface FileUpdater {
	
	/**
	 * 更新输入的文件，更新后保存在指定的输出文件。
	 * 输入文件和输入文件可以相同。
	 * 
	 * @param input 输入文件
	 * @param output 输出文件
	 */
	void update(File input, File output);
	
//	/**
//	 * 更新文件，更新后保存到原文件。
//	 * @param file
//	 */
//	void update(File file);
}
